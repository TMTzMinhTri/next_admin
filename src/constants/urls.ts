const urls = {
  login: () => '/login',
  register: () => '/register',
  excel: () => '/excel'

  // clientDetail: (slug: string) => ({
  //   href: '/clients/[slug]',
  //   as: `/clients/${slug}`,
  // }),
}

export default urls
